MONGODB_URI=mongodb://localhost:27017/easypoints
JWT_SECRET=easypoints-hackathon-secret-2026
JWT_EXPIRES_IN=7d
PORT=3000

# QR HMAC signing secret (defaults to JWT_SECRET if not set)
QR_HMAC_SECRET=

# Email / SMTP (for magic link auth + notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@easypoints.app

# CORS — comma-separated allowed origins (leave empty for dev = allow all)
CORS_ORIGINS=
